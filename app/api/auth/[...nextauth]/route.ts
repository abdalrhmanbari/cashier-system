// هذا المسار متوقف — المصادقة تمت نقلها إلى:
//   /api/super-admin-auth/[...nextauth]  ← مدير النظام
//   /api/store-auth/[...nextauth]        ← مستخدمو المتاجر
export async function GET() {
  return new Response('Not found', { status: 404 })
}
export async function POST() {
  return new Response('Not found', { status: 404 })
}
